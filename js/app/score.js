/* const scoreList = document.querySelector('.scores'); */
console.log(scoreList, 'AAAAAAAAAAAAAAAAAA');

/* export const setupScores = (data) => {
    if (data.length){

        let html = '';
        data.forEach(doc => {
            console.log(doc);
            const score = doc.data();

            const li =  `
                <li>
                    <h5>${score.score}</h5>
                </li>
            `
            html += li;
        });


        scoreList.innerHTML = html;

        console.log('Loop scores');
    } else {
        console.log('No hay scores')
        scoreList.innerHTML = '<h2>No hay puntajes aún</h2>';
    }
} */